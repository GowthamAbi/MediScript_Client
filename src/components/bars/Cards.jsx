import React from 'react'

export default function Cards({item,index}) {
  return (
    <div>
        <div>
            <img src="" alt={`${index}`} />
            <h1>{item}</h1>
        </div>
        
        </div>
  )
}
