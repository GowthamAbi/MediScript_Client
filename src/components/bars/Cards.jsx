import React from 'react'

export default function Cards({item,index}) {
  return (
    <div>
        <div className='' > 
            <img src="" alt={`${index}`} />
            <h1 className='text-wrap'>{item}</h1>
        </div>
        
        </div>
  )
}
